# Documentation Index

Documentation is split by ownership.

- `architecture/` contains cross-project structure and historical system context.
- `operations/` contains deployment, server operation, and Git notes.
- Product-specific documentation belongs under `projects/<product>/docs/`.

Keep root-level Markdown files out of the repository root unless they are required by external tooling.
