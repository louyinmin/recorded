# Life Atlas Docs

Life Atlas owns:

- Frontend pages and assets under `projects/life_atlas/frontend/`.
- Backend implementation under `projects/life_atlas/backend/life_backend/`.
- Public browser routes `/login.html` and `/life.html`.
- API routes under `/api/life/*`.

The root `life_backend/` package is only a compatibility entry for existing imports.
