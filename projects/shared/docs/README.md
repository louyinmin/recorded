# Shared Docs

Shared owns code and assets used by multiple product surfaces.

- `frontend/assets/` contains shared browser assets such as the common login and travel stylesheet.
- `backend/wechat_backend/` contains shared WeChat Mini Program session handling for NBA and Timing.

The root `wechat_backend/` package is only a compatibility entry for existing imports.
