# Timing API Docs

Timing API owns:

- Backend implementation under `projects/timing_api/backend/timing_backend/`.
- Mini Program API routes under `/api/timing/*`.
- Plan config, task config, and timing stats endpoints.

The root `timing_backend/` package is only a compatibility entry for existing imports.
